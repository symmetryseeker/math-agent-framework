- insert:
    - id: math-agent
      name: 'dsh-math-agent'
      config: {}
      # 配置说明（任选其一，勿硬编码本机路径）:
      #  1) 环境变量 MATH_AGENT_HOME=<MAF 仓库根目录>  （推荐，可移植）
      #  2) --patch 覆盖本 config:
      #       - id: math-agent
      #         config:
      #           mathAgentHome: '<MAF 仓库根目录>'
      #           pythonCmd: 'python'          # 可选，默认 python / MATH_AGENT_PYTHON
