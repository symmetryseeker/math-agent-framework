- insert:
    - id: math-agent
      name: 'dsh-math-agent'
      config:
        # 指向 Math Agent Framework 仓库根目录（bridge.py 所在处）
        # 或用环境变量 MATH_AGENT_HOME 覆盖
        mathAgentHome: 'D:/tools/math-agent-framework'
        # pythonCmd: 'python'   # 可覆盖；或用环境变量 MATH_AGENT_PYTHON
